name: Release source package

on:
  push:
    tags: ["v*"]

permissions:
  contents: read

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      PACKAGE_NAME: "path"
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          repository: moglang/mog
          ref: main
          path: .runtime/mog
      - name: Verify tag and manifest version
        shell: bash
        run: |
          set -euo pipefail
          version="$(sed -n 's/^version = "\([^"]*\)"/\1/p' mog.toml | head -n 1)"
          test -n "$version"
          test "${GITHUB_REF_NAME#v}" = "$version"
          grep -Fq "## $version" CHANGELOG.md
      - name: Build Mog runtime
        run: |
          cmake -S .runtime/mog -B .runtime/mog/build -DCMAKE_BUILD_TYPE=Release
          cmake --build .runtime/mog/build --parallel
      - name: Validate package and run tests
        shell: bash
        run: |
          set -euo pipefail
          runtime="$GITHUB_WORKSPACE/.runtime/mog/build/interpreter"
          version="${GITHUB_REF_NAME#v}"
          stage="$RUNNER_TEMP/stage/github.com/moglang/$PACKAGE_NAME"
          project="$RUNNER_TEMP/package-test"
          mkdir -p "$(dirname "$stage")" "$project"
          rsync -a --exclude .git --exclude .runtime "$GITHUB_WORKSPACE/" "$stage/"
          "$runtime" validate-package "$stage"
          sed -e "s|__PACKAGE_NAME__|$PACKAGE_NAME|g" -e "s|__PACKAGE_VERSION__|$version|g" -e "s|__PACKAGE_PATH__|$stage|g" \
            "$GITHUB_WORKSPACE/.github/package-test.toml.in" > "$project/mog.toml"
          (
            cd "$project"
            MOG_CACHE_DIR="$RUNNER_TEMP/mog-cache" "$runtime" run "$stage/tests/main.mog"
          )
          for fixture in "$stage"/tests/errors/*.mog; do
            [[ -e "$fixture" ]] || continue
            if (
              cd "$project"
              MOG_CACHE_DIR="$RUNNER_TEMP/mog-cache" "$runtime" run "$fixture"
            ); then
              echo "Expected failure fixture succeeded: $fixture" >&2
              exit 1
            fi
          done

  release:
    needs: validate
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: write
    env:
      PACKAGE_NAME: "path"
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - name: Bundle package and generate checksum
        shell: bash
        run: |
          set -euo pipefail
          version="${GITHUB_REF_NAME#v}"
          bundle="$RUNNER_TEMP/$PACKAGE_NAME-$version"
          mkdir -p "$bundle"
          rsync -a --exclude .git --exclude .github --exclude build ./ "$bundle/"
          tar -C "$RUNNER_TEMP" -czf "$PACKAGE_NAME-$version.tar.gz" "$PACKAGE_NAME-$version"
          sha256sum "$PACKAGE_NAME-$version.tar.gz" > SHA256SUMS
      - uses: softprops/action-gh-release@3bb12739c298aeb8a4eeaf626c5b8d85266b0e65 # v2
        with:
          files: |
            path-*.tar.gz
            SHA256SUMS
          fail_on_unmatched_files: true
