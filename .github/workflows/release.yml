name: Release source package

on:
  push:
    tags: ["v*"]

permissions:
  contents: write

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  release:
    uses: moglang/package-actions/.github/workflows/source-release.yml@v1
    with:
      package_name: path
      runtime_ref: main
      test_command: |
        for fixture in "$PACKAGE_STAGE"/tests/errors/*.mog; do
          [[ -e "$fixture" ]] || continue
          if MOG_CACHE_DIR="$RUNNER_TEMP/mog-cache" "$MOG_RUNTIME" run "$fixture"; then
            echo "Expected failure fixture succeeded: $fixture" >&2
            exit 1
          fi
        done
