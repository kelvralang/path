name: Package CI

on:
  pull_request:
  push:
    branches: [main]
    tags: ["v*"]

permissions:
  contents: read

concurrency:
  group: package-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  current-runtime:
    uses: kelvralang/package-actions/.github/workflows/source-ci.yml@v1
    with:
      package_name: path
      runtime_ref: main
      test_command: |
        for fixture in "$PACKAGE_STAGE"/tests/errors/*.kel; do
          [[ -e "$fixture" ]] || continue
          if KELVRA_CACHE_DIR="$RUNNER_TEMP/kelvra-cache" "$KELVRA_RUNTIME" run "$fixture"; then
            echo "Expected failure fixture succeeded: $fixture" >&2
            exit 1
          fi
        done
